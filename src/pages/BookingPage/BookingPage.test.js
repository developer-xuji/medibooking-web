import React from 'react';
import { shallow } from 'enzyme';
import BookingPage from './BookingPage';
import BookingDetails from "./components/BookingDetails";
import TimeSelector from "./components/TimeSelector";
import DoctorSelector from "./components/DoctorSelector";
import DateSelector from "./components/DateSelector";

describe('<BookingPage />', () => {
    it('renders', () => {  
        // const wrapper = shallow(<BookingPage />);

        // expect(wrapper.find(BookingDetails)).toHaveLength(1);
        // expect(wrapper.find(TimeSelector)).toHaveLength(1);
        // expect(wrapper.find(DoctorSelector)).toHaveLength(1);
        // expect(wrapper.find(DateSelector)).toHaveLength(1);
    });
});