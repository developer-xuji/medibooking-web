import styled from "styled-components";
import { AVATAR_ICON } from "../../../../../../constants";

const Avatar = styled.div`
  background: url(${AVATAR_ICON});
  background-size: cover;
  height: 50px;
  width: 50px;
`;

export default Avatar;
